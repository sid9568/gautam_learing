
<div class="container-fluid bg-light min-vh-100 d-flex align-items-center justify-content-center ">
  <div class="col-md-2 mb-auto mt-5">
    <div class="text-center">
     <div>

            <h1 class="logo-name">IN+</h1>

        </div>
        <h3>Welcome to IN+</h3>
        <p>Perfectly designed and precisely prepared admin theme with over 50 pages with extra new web app views.
            <!--Continually expanded and constantly improved Inspinia Admin Them (IN+)-->
        </p>
        <p>Login in. To see it in action.</p>

      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

        <div class="form-group">
          <%= f.email_field :email, autofocus: true,placeholder: "Username", autocomplete: "user_email", class: "form-control",id:"user_email" %>
        </div><br>

        <div class="form-group">
          <%= f.password_field :password, autocomplete: "current-password",placeholder: "Password", class: "form-control" %>
        </div><br>

      

        <div class="actions">
          <%= f.submit "Log in", class: "btn btn-primary block full-width m-b", data: {"turbo-frame": "page_body"}%>
        </div>
      <% end %>

      <%#= render "devise/shared/links" %>
    </div>
  </div>
</div>
